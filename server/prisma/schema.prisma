generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Habits {
  id         String   @id @default(uuid())
  title      String
  created_at DateTime

  @@map("habits")
}
